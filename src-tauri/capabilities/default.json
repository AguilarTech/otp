{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum permissions for the main window: app data dir resolution, event emission to the webview, a directory-only file picker for USB pairing, and a save dialog for .otp / attachment downloads.",
  "windows": ["main"],
  "permissions": [
    "core:app:default",
    "core:event:default",
    "core:path:default",
    "core:window:default",
    "core:webview:default",
    "dialog:allow-open",
    "dialog:allow-save"
  ]
}
